# Memory for the Gradle daemon; AGP 8 is happier with 2 GB than the 512 MB default.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Only this module's own resources go into its R class (there are no libraries anyway).
android.nonTransitiveRClass=true
