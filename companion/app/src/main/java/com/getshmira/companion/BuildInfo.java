package com.getshmira.companion;

/** Version string sent as the User-Agent of the policy fetch. Kept in step with app/build.gradle. */
final class BuildInfo {
    static final String VERSION = "0.2.0";

    private BuildInfo() {
    }
}
